import React from 'react'
import ReactDOM from 'react-dom/client'
import Autoversion from './Autoversion.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Autoversion />
  </React.StrictMode>,
)